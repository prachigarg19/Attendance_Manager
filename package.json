{
  "name": "attendance-manager",
  "ProductName": "\"attendance manager\"",
  "version": "1.0.2",
  "description": "\"attendance manager\"",
  "main": "main.js",
  "scripts": {
    "package-win": "electron-packager . attendance-manager --overwrite --asar=true --platform=win32 --arch=ia32 --icon=logo/logo.png logo/logo.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"attendance manager\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^12.2.3",
    "electron-packager": "^15.2.0"
  },
  "author": "Prachi Garg",
  "license": "MIT",
  "dependencies": {
    "xlsx": "^0.17.0"
  }
}
